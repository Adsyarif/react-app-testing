import { createContext } from "react";

interface User {
  firstName: string;
  lastName: string;
  email: string;
}

interface Context

const defaultValue = {};

const AppProvider = createContext(defaultValue);

const  
